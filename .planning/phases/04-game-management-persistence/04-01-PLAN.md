---
phase: 04-game-management-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/index.ts, src/store/useGameStore.ts]
autonomous: true
requirements: [PH4-04, PH4-05, PH4-07]

must_haves:
  truths:
    - "Game state persists across page refreshes"
    - "Mistake count increments when an invalid number is placed"
    - "Personal bests are stored in the state"
  artifacts:
    - path: "src/store/useGameStore.ts"
      provides: "Zustand store with persist middleware"
      contains: "persist"
    - path: "src/types/index.ts"
      provides: "Updated GameState and PB types"
  key_links:
    - from: "src/store/useGameStore.ts"
      to: "LocalStorage"
      via: "Zustand persist middleware"
---

<objective>
Establish the core state management for game persistence, mistake tracking, and personal bests.

Purpose: Provide a reliable foundation for long-term progress and feedback.
Output: Updated Zustand store with persistence middleware and new state tracking fields.
</objective>

<execution_context>
@/home/code/.gemini/get-shit-done/workflows/execute-plan.md
@/home/code/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/04-CONTEXT.md
@src/types/index.ts
@src/store/useGameStore.ts

<interfaces>
From src/types/index.ts:
```typescript
export type Difficulty = 'easy' | 'medium' | 'hard' | 'expert';
export interface Cell {
  value: number | null;
  fixed: boolean;
  notes: number[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GameState and PB Types</name>
  <files>src/types/index.ts</files>
  <action>
    - Add `timer: number` (seconds).
    - Add `isPaused: boolean`.
    - Add `mistakeCount: number`.
    - Add `bestTimes: Record<Difficulty, { time: number; mistakes: number } | null>`.
    - Add `isGameWon: boolean`.
    - Add actions to `GameState`:
      - `tickTimer: () => void`
      - `togglePause: (paused?: boolean) => void`
      - `incrementMistakes: () => void`
      - `setGameWon: (won: boolean) => void`
      - `saveBestTime: () => void` (checks if current is better)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Types updated with all required fields for Phase 4.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Zustand Persist & Actions</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    - Import `persist` and `createJSONStorage` from `zustand/middleware`.
    - Wrap the store in `persist` with `name: 'sudoku-storage'`.
    - Implement `tickTimer`: increment state.timer by 1.
    - Implement `togglePause`: flip isPaused or set to provided value.
    - Implement `incrementMistakes`: increment mistakeCount.
    - Implement `setGameWon`: set isGameWon.
    - Implement `saveBestTime`: if current time < PB time (or PB is null), update PB.
    - Update `initGame` to reset timer, mistakeCount, and isPaused.
    - Integrate versioning: add `version: 1` to persist options.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Store implements persistence and all new state management actions.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Mistake Tracking in setCellValue</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    - Update `setCellValue` to detect if the placed value causes a conflict.
    - If `value !== null` and `state.settings.instantFeedback` is on:
      - Get current conflicts using `getAllConflicts`.
      - Check if the placed `index` is now in the conflicts list.
      - If yes, call `incrementMistakes()`.
    - Ensure mistakeCount only increments for the new placement, not for existing ones.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>setCellValue correctly tracks mistakes when invalid numbers are entered.</done>
</task>

</tasks>

<verification>
Check `localStorage.getItem('sudoku-storage')` in browser console to verify state persistence.
Verify `mistakeCount` increments in React DevTools when a conflict is created.
</verification>

<success_criteria>
- Game state persists across browser reloads.
- Mistake counter accurately tracks invalid entries.
- Personal best storage structure exists and is ready for data.
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-management-persistence/04-01-SUMMARY.md`
</output>
