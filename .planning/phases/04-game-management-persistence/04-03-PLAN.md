---
phase: 04-game-management-persistence
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [src/components/ui/NewGameModal.tsx, src/components/grid/SudokuCell.tsx, src/index.css, src/store/useGameStore.ts]
autonomous: false
requirements: [PH4-02, PH4-03, PH4-06, PH4-10]

must_haves:
  truths:
    - "Game detects and celebrates a correct solution"
    - "New Game modal allows difficulty selection and shows PBs"
    - "Monochrome Zen aesthetic is applied across the UI"
    - "Subtle animations play on number placement"
  artifacts:
    - path: "src/components/ui/NewGameModal.tsx"
      provides: "New game flow with settings and confirmation"
    - path: "src/index.css"
      provides: "Zen Monochrome styles"
  key_links:
    - from: "src/store/useGameStore.ts"
      to: "isGameWon"
      via: "isGameSolved check in setCellValue"
---

<objective>
Finalize the game flow with a dedicated New Game experience, win detection, and "Zen Minimalism" aesthetics.

Purpose: Provide a polished entry and exit for the game and ensure a cohesive visual experience.
Output: Win celebration logic, a comprehensive New Game modal, and refined visual styling.
</objective>

<execution_context>
@/home/code/.gemini/get-shit-done/workflows/execute-plan.md
@/home/code/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/04-CONTEXT.md
@src/index.css
@src/components/grid/SudokuCell.tsx
@src/store/useGameStore.ts

<interfaces>
From src/store/useGameStore.ts:
```typescript
initGame: (difficulty: Difficulty) => void;
saveBestTime: () => void;
isGameWon: boolean;
grid: Cell[];
difficulty: Difficulty;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Win Detection & Celebration</name>
  <files>src/store/useGameStore.ts, src/components/ui/WinOverlay.tsx</files>
  <action>
    - Update `setCellValue`: If `value !== null`, check if the grid is now solved.
    - Grid is solved if every cell has a `value` and `getAllConflicts` returns an empty array.
    - If solved, set `isGameWon` to true and call `saveBestTime()`.
    - Create a subtle `WinOverlay` component that fades in with a message like "Puzzle Solved" and shows final stats (time, mistakes).
    - Add a button in the overlay to start a "New Game".
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Game correctly identifies a win and displays a celebratory overlay.</done>
</task>

<task type="auto">
  <name>Task 2: New Game Modal with Settings</name>
  <files>src/components/ui/NewGameModal.tsx, src/App.tsx</files>
  <action>
    - Create a centered `NewGameModal`.
    - Features:
      - Difficulty Selection buttons (Easy, Medium, Hard, Expert).
      - PB display for each difficulty (fetched from `bestTimes` store).
      - Symmetry Toggle (Checkbox, label: "Symmetrical Layout", placeholder functionality).
      - "Start Game" button.
    - Implementation:
      - If user clicks "Start Game" and current board has progress (any non-fixed cell is filled), show a "Are you sure?" confirmation.
      - On confirm, call `initGame(selectedDifficulty)`.
    - Update `App.tsx` to show this modal on first load OR when triggered by a button.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>New game flow is robust, handles confirmations, and displays personal records.</done>
</task>

<task type="auto">
  <name>Task 3: Zen Aesthetics & Animations</name>
  <files>src/index.css, src/components/grid/SudokuCell.tsx</files>
  <action>
    - Update `index.css`:
      - Switch palette to true monochrome (pure whites, blacks, and distinct gray shades for highlights).
      - Use subtle borders and soft shadows.
    - Update `SudokuCell.tsx`:
      - Add a CSS animation (using a class or library like Framer Motion if already in project, but pure CSS preferred) for the number value.
      - Trigger a fast fade-in and subtle scale-up (e.g., from 0.8 to 1.0) when `value` changes from null to a number.
    - Ensure all UI elements (Keypad, GameInfo) follow the monochrome theme.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The application feels "Zen" with minimalist styling and responsive, subtle animations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full game management flow and Zen UI</what-built>
  <how-to-verify>
    1. Start a new game and check the modal.
    2. Place a number and observe the subtle scale/fade animation.
    3. Fill the grid (or mock a win state) and verify the celebration.
    4. Check the visual style against the "Zen Minimalism" requirements.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manually solve a puzzle (or trigger `isGameWon` manually) and verify the win overlay.
Check color contrast for monochrome palette.
</verification>

<success_criteria>
- User can select difficulty and see PBs.
- New game confirmation prevents accidental progress loss.
- UI is aesthetically pleasing and minimalist.
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-management-persistence/04-03-SUMMARY.md`
</output>
