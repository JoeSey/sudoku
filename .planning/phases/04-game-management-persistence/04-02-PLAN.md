---
phase: 04-game-management-persistence
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/App.tsx, src/components/grid/SudokuGrid.tsx, src/components/ui/GameInfo.tsx]
autonomous: true
requirements: [PH4-01, PH4-08, PH4-09]

must_haves:
  truths:
    - "Timer increments every second while game is active"
    - "Game pauses automatically when tab loses focus"
    - "Sudoku grid is obscured while game is paused"
  artifacts:
    - path: "src/components/ui/GameInfo.tsx"
      provides: "Display for Timer and Mistake Counter"
    - path: "src/App.tsx"
      provides: "Timer lifecycle and focus management"
  key_links:
    - from: "src/App.tsx"
      to: "tickTimer"
      via: "setInterval in useEffect"
    - from: "window"
      to: "togglePause"
      via: "visibilitychange event listener"
---

<objective>
Implement time tracking and intelligent pause behavior to ensure a fair and convenient game lifecycle.

Purpose: Prevent solving during pauses and provide clear feedback on time elapsed.
Output: Active timer, auto-pause behavior, and an information display for game stats.
</objective>

<execution_context>
@/home/code/.gemini/get-shit-done/workflows/execute-plan.md
@/home/code/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/04-CONTEXT.md
@src/App.tsx
@src/store/useGameStore.ts

<interfaces>
From src/store/useGameStore.ts (after Plan 01):
```typescript
tickTimer: () => void;
togglePause: (paused?: boolean) => void;
timer: number;
mistakeCount: number;
isPaused: boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timer Logic & Auto-Pause</name>
  <files>src/App.tsx</files>
  <action>
    - Add a `useEffect` to manage a 1-second interval calling `tickTimer()` ONLY if `isPaused` and `isGameWon` are false.
    - Add a `useEffect` to listen for `visibilitychange` and `blur`/`focus` events.
    - Call `togglePause(true)` on `visibilitychange` (if hidden) or `blur`.
    - Auto-pause only if game is currently active and not already won.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Timer increments correctly and pauses when the tab loses focus.</done>
</task>

<task type="auto">
  <name>Task 2: GameInfo Component</name>
  <files>src/components/ui/GameInfo.tsx</files>
  <action>
    - Create a simple component to display the current `timer` (formatted as MM:SS) and `mistakeCount`.
    - Style it according to "Zen Minimalism" (clean, monochrome).
    - Add a "Pause/Resume" button that toggles `isPaused`.
    - Mount this component in `App.tsx` above the grid.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Timer and mistakes are visible on the screen with formatted output.</done>
</task>

<task type="auto">
  <name>Task 3: Grid Hiding & Blur</name>
  <files>src/components/grid/SudokuGrid.tsx</files>
  <action>
    - Access `isPaused` from the store.
    - If `isPaused` is true, wrap the grid content in a div that applies a strong blur or obscures it entirely.
    - Provide a "Click to Resume" overlay centered over the blurred grid.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Grid is obscured while paused, preventing the user from solving.</done>
</task>

</tasks>

<verification>
Start a game, wait 5 seconds, switch tabs, wait 5 seconds, switch back. Verify timer is at 5-6 seconds, not 10.
Verify grid numbers are hidden when the "Pause" button is clicked.
</verification>

<success_criteria>
- Timer works correctly in background.
- UI displays game status clearly.
- No possibility to see the puzzle during pause.
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-management-persistence/04-02-SUMMARY.md`
</output>
