---
phase: 02-solving-interface
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/index.ts, src/store/useGameStore.ts, src/components/grid/SudokuGrid.tsx, src/components/grid/SudokuCell.tsx]
autonomous: true
requirements: [PH2-01]

must_haves:
  truths:
    - "User can click a cell to select it"
    - "Arrow keys move selection with Grid Warp"
    - "Tab key moves selection skipping fixed cells"
    - "Selected cell persists even when clicking outside the grid"
  artifacts:
    - path: "src/store/useGameStore.ts"
      provides: "Selection state and navigation logic"
  key_links:
    - from: "src/components/grid/SudokuGrid.tsx"
      to: "useGameStore.moveSelection"
      via: "onKeyDown event handler"
---

<objective>
Implement cell selection logic and keyboard navigation (Grid Warp).
Purpose: Make the grid interactive and accessible via keyboard.
Output: Working selection state and navigation logic.
</objective>

<execution_context>
@/home/code/.gemini/get-shit-done/workflows/execute-plan.md
@/home/code/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASE_2/RESEARCH.md
@.planning/02-CONTEXT.md

<interfaces>
From src/types/index.ts:
```typescript
export interface GameState {
  grid: Cell[];
  difficulty: Difficulty;
  initGame: (difficulty: Difficulty) => void;
  setCellValue: (index: number, value: number | null) => void;
  toggleNote: (index: number, note: number) => void;
  validateGrid: () => boolean;
  resetGame: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Store with Selection State</name>
  <files>src/types/index.ts, src/store/useGameStore.ts</files>
  <action>
    - Add `selectedCellIndex: number | null` to `GameState` interface in `src/types/index.ts`.
    - Add `setSelectedCellIndex: (index: number | null) => void` and `moveSelection: (direction: 'Up' | 'Down' | 'Left' | 'Right' | 'Tab' | 'ShiftTab') => void` to `GameState` interface.
    - Implement `setSelectedCellIndex` and `moveSelection` in `src/store/useGameStore.ts`.
    - Grid Warp logic for `moveSelection`:
      - Right: `(col + 1) % 9`
      - Left: `(col - 1 + 9) % 9`
      - Down: `(row + 1) % 9`
      - Up: `(row - 1 + 9) % 9`
    - Tab logic: Iterate forward (or backward for Shift+Tab) until a non-fixed cell is found, or warp around.
  </action>
  <verify>
    <automated>Check types and store for new fields and methods</automated>
  </verify>
  <done>Selection state and navigation logic implemented in the store</done>
</task>

<task type="auto">
  <name>Task 2: Implement Interaction in Grid and Cell</name>
  <files>src/components/grid/SudokuGrid.tsx, src/components/grid/SudokuCell.tsx</files>
  <action>
    - Update `SudokuGrid.tsx`:
      - Add `onKeyDown` handler to the grid container.
      - Map arrow keys to `moveSelection`.
      - Map `Tab` (and `Shift+Tab`) to `moveSelection`.
    - Update `SudokuCell.tsx`:
      - Add `onClick` to set `selectedCellIndex`.
      - Use `isSelected` selector to apply `selected` class.
      - Use Roving TabIndex: `tabIndex={isSelected ? 0 : -1}`.
      - Add a `useEffect` to focus the cell when `isSelected` becomes true.
  </action>
  <verify>
    <automated>Check that SudokuCell has tabIndex and onClick</automated>
  </verify>
  <done>Selection and navigation functional in the UI</done>
</task>

</tasks>

<verification>
- Select a cell by clicking.
- Move selection using arrow keys, confirming Grid Warp (e.g., Right at col 8 goes to col 0).
- Press Tab to move between non-fixed cells.
- Confirm selection persists when clicking outside the grid (if focus remains or logic is persistent).
</verification>

<success_criteria>
- Selection state is correctly managed in Zustand.
- Keyboard navigation works across all directions with warp logic.
- Tab key correctly skips fixed cells.
</success_criteria>

<output>
After completion, create `.planning/phases/02-solving-interface/02-01-SUMMARY.md`
</output>
