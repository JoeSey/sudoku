---
phase: 02-solving-interface
plan: 02-02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/components/grid/SudokuCell.tsx, src/index.css]
autonomous: true
requirements: [PH2-02, PH2-03, PH2-04]

must_haves:
  truths:
    - "User can enter numbers 1-9 into selected cell"
    - "User can delete value using Backspace or Delete"
    - "Selected cell's row, column, and block are highlighted (crosshair)"
    - "All cells with the same value as the selected cell are highlighted"
  artifacts:
    - path: "src/index.css"
      provides: "Highlighting styles (crosshair and identical numbers)"
  key_links:
    - from: "src/components/grid/SudokuCell.tsx"
      to: "useGameStore.setCellValue"
      via: "onKeyDown event handler"
---

<objective>
Implement number input and visual feedback (crosshair and identical number highlights).
Purpose: Enable solving the puzzle and provide visual guidance.
Output: Working number entry and multi-layered highlights.
</objective>

<execution_context>
@/home/code/.gemini/get-shit-done/workflows/execute-plan.md
@/home/code/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASE_2/RESEARCH.md
@.planning/02-CONTEXT.md
@.planning/phases/02-solving-interface/02-01-SUMMARY.md

<interfaces>
From src/types/index.ts:
```typescript
export interface Cell {
  value: number | null;
  fixed: boolean;
  notes: number[];
}
```

From src/store/useGameStore.ts:
```typescript
setCellValue: (index: number, value: number | null) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handle Keyboard Input</name>
  <files>src/components/grid/SudokuCell.tsx</files>
  <action>
    - Update `SudokuCell.tsx` to handle number keys (1-9) and deletion keys (Backspace, Delete, Digit0).
    - Call `setCellValue(index, value)` appropriately.
    - Ensure input is only allowed if the cell is NOT fixed.
  </action>
  <verify>
    <automated>Check that SudokuCell has an onKeyDown for number input</automated>
  </verify>
  <done>Number input and deletion functional in the UI</done>
</task>

<task type="auto">
  <name>Task 2: Implement Multi-Layered Highlighting</name>
  <files>src/components/grid/SudokuCell.tsx, src/index.css</files>
  <action>
    - Add selectors in `SudokuCell.tsx` for:
      - `isRelated`: Cell is in the same row, column, or block as `selectedCellIndex`.
      - `isIdentical`: Cell has the same non-null value as `grid[selectedCellIndex]`.
    - Apply CSS classes `related` and `identical` to the cell.
    - Update `src/index.css` with styling for:
      - `.selected`: Strongest highlight (e.g., accent color background).
      - `.related`: Crosshair highlight (e.g., subtle light background).
      - `.identical`: Secondary highlight (e.g., different tint background or outline).
  </action>
  <verify>
    <automated>Verify CSS classes are applied to SudokuCell based on store state</automated>
  </verify>
  <done>Visual highlighting functional</done>
</task>

</tasks>

<verification>
- Select an empty cell and enter numbers 1-9.
- Use Backspace/Delete to clear a cell.
- Confirm highlights:
  - Active cell crosshair (row, col, block) updates as selection moves.
  - Identical number highlights update as selection moves or values change.
</verification>

<success_criteria>
- Number entry works for all non-fixed cells.
- Highlighting correctly identifies related cells and identical values.
- Performance remains fluid (Zustand selectors used correctly).
</success_criteria>

<output>
After completion, create `.planning/phases/02-solving-interface/02-02-SUMMARY.md`
</output>
