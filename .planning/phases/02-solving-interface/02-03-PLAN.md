---
phase: 02-solving-interface
plan: 02-03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [src/utils/sudokuUtils.ts, src/store/useGameStore.ts, src/components/grid/SudokuCell.tsx]
autonomous: true
requirements: [PH2-05]

must_haves:
  truths:
    - "Numbers conflict if they are identical and in the same row, column, or block"
    - "Conflict is visually indicated (e.g., red value color)"
    - "Conflict clears immediately when the duplicate is changed or deleted"
  artifacts:
    - path: "src/utils/sudokuUtils.ts"
      provides: "Conflict detection logic"
  key_links:
    - from: "src/components/grid/SudokuCell.tsx"
      to: "sudokuUtils.getConflicts"
      via: "Zustand selector"
---

<objective>
Implement real-time conflict detection and visual feedback.
Purpose: Alert the user to illegal moves immediately.
Output: Conflict highlighting in the grid.
</objective>

<execution_context>
@/home/code/.gemini/get-shit-done/workflows/execute-plan.md
@/home/code/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASE_2/RESEARCH.md
@.planning/02-CONTEXT.md
@.planning/phases/02-solving-interface/02-02-SUMMARY.md

<interfaces>
From src/types/index.ts:
```typescript
export interface Cell {
  value: number | null;
  fixed: boolean;
  notes: number[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conflict Detection Logic</name>
  <files>src/utils/sudokuUtils.ts</files>
  <action>
    - Add `getCellConflicts(grid: Cell[], index: number): number[]` to `src/utils/sudokuUtils.ts`.
    - This function should return indices of all cells in the same row, col, or block as `index` that have the same `value` (if value is not null).
    - Add `getAllConflicts(grid: Cell[]): Set<number>` to identify all cells currently in conflict.
  </action>
  <verify>
    <automated>Check for new utility functions in sudokuUtils.ts</automated>
  </verify>
  <done>Conflict detection logic implemented in utilities</done>
</task>

<task type="auto">
  <name>Task 2: Conflict Highlighting</name>
  <files>src/store/useGameStore.ts, src/components/grid/SudokuCell.tsx</files>
  <action>
    - Update `SudokuCell.tsx` to use a selector that checks if the cell index is in conflict.
    - Note: To avoid re-calculating everything on every change, we can use a selector that only checks row/col/block for the current cell, or pre-calculate conflicts in the store on `setCellValue`. 
    - Per user decision: "Instant clear on fix".
    - Update `SudokuCell.tsx` to apply the `conflict` class.
    - Update `src/index.css` (or `SudokuCell.tsx` styles) to style the conflict (e.g., `color: var(--error-red)` for the cell value).
  </action>
  <verify>
    <automated>Verify the conflict class is applied when a duplicate is entered</automated>
  </verify>
  <done>Visual conflict feedback functional</done>
</task>

</tasks>

<verification>
- Enter a number that conflicts with a fixed number in the same row. Confirm it turns red.
- Enter a number that conflicts with a user-entered number in the same block. Confirm both turn red (if implementing bi-directional highlighting).
- Delete one of the conflicting numbers. Confirm the conflict clears immediately.
</verification>

<success_criteria>
- All illegal Sudoku moves are flagged with visual feedback.
- Conflict detection is real-time and clears instantly upon correction.
- Performance remains fluid for 81 cells.
</success_criteria>

<output>
After completion, create `.planning/phases/02-solving-interface/02-03-SUMMARY.md`
</output>
