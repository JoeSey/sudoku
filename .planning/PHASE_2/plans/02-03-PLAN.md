---
phase: 02-solving-interface
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: 
  - src/utils/sudokuUtils.ts
  - src/store/useGameStore.ts
  - src/components/grid/SudokuCell.tsx
  - src/index.css
autonomous: true
requirements: [PH2-05]
must_haves:
  truths:
    - "Duplicate numbers in a row/col/block are visually marked."
    - "Conflict markers clear instantly when the conflict is resolved."
  artifacts:
    - path: "src/utils/sudokuUtils.ts"
      provides: "conflict detection utility"
---

<objective>
Implement conflict detection and visual feedback for invalid moves.
Prioritize instant feedback and clear visual cues.

Purpose: Help users identify errors in their logic.
Output: A grid that highlights conflicting numbers in real-time.
</objective>

<context>
@.planning/PHASE_2/RESEARCH.md
@.planning/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Configurable Conflict Detection</name>
  <files>src/utils/sudokuUtils.ts, src/store/useGameStore.ts</files>
  <action>
    In `sudokuUtils.ts`, implement `getConflicts(grid: Cell[], index: number): number[]`.
    
    Update `useGameStore.ts`:
    - Add `settings: { instantFeedback: boolean }` to state.
    - Add `conflicts: number[]` to state.
    - Update `setCellValue` to conditionally update `conflicts` if `settings.instantFeedback` is true.
    - Add a `checkConflicts()` action to manually trigger validation if feedback is not instant.
  </action>
  <verify>
    <automated>grep "instantFeedback" src/store/useGameStore.ts</automated>
  </verify>
  <done>Conflicts are identified and controlled by user settings.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Visual Conflict Feedback</name>
  <files>src/components/grid/SudokuCell.tsx, src/index.css</files>
  <action>
    Update `SudokuCell.tsx`:
    - Use a selector to determine `isConflicting`.
    - Apply a `.conflict` class.
    
    Update `index.css`:
    - Implement "Text-Only Override" for conflicts: the number itself turns red.
    - Ensure it overrides other highlights as per CONTEXT.md.
  </action>
  <verify>
    <automated>grep "conflict" src/components/grid/SudokuCell.tsx</automated>
  </verify>
  <done>Conflicts are visually marked in the grid.</done>
</task>

</tasks>

<success_criteria>
- Entering a duplicate number in a row, column, or block triggers a conflict highlight.
- Conflict highlights are text-only (red numbers) and override other highlights.
- Conflict highlights clear immediately when the conflict is resolved.
</success_criteria>
