---
phase: 02-solving-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - src/types/index.ts
  - src/store/useGameStore.ts
  - src/components/grid/SudokuGrid.tsx
  - src/components/grid/SudokuCell.tsx
autonomous: true
requirements: [PH2-01, PH2-02]
must_haves:
  truths:
    - "Clicking a cell selects it."
    - "Arrow keys navigate between cells."
    - "Grid Warp: Right at col 8 jumps to col 0."
  artifacts:
    - path: "src/store/useGameStore.ts"
      provides: "selection state and moveSelection action"
  key_links:
    - from: "src/components/grid/SudokuGrid.tsx"
      to: "src/store/useGameStore.ts"
      via: "onKeyDown event handler"
---

<objective>
Implement the core selection logic and keyboard navigation.
Prioritize fluid navigation with the "Grid Warp" behavior.

Purpose: Foundation for all solving interactions.
Output: A grid where cells can be selected and navigated via keyboard.
</objective>

<context>
@.planning/PHASE_2/RESEARCH.md
@.planning/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Store with Selection State</name>
  <files>src/types/index.ts, src/store/useGameStore.ts</files>
  <action>
    Update `GameState` type to include `selectedCellIndex: number | null`.
    Add `setSelectedCellIndex(index: number | null)` to store actions.
    Add `moveSelection(direction: 'up' | 'down' | 'left' | 'right')` to store actions.
    - Implement "Grid Warp" arithmetic:
      - Right: `(col + 1) % 9`
      - Left: `(col - 1 + 9) % 9`
      - Down: `(row + 1) % 9`
      - Up: `(row - 1 + 9) % 9`
  </action>
  <verify>
    <automated>npx vite-node -e "import { useGameStore } from './src/store/useGameStore'; useGameStore.getState().setSelectedCellIndex(0); useGameStore.getState().moveSelection('right'); if (useGameStore.getState().selectedCellIndex !== 1) process.exit(1); console.log('Selection move success');"</automated>
  </verify>
  <done>Selection state and movement logic implemented in store.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Grid Navigation Handlers</name>
  <files>src/components/grid/SudokuGrid.tsx, src/components/grid/SudokuCell.tsx</files>
  <action>
    Update `SudokuGrid.tsx`:
    - Add a central `onKeyDown` handler to the grid container.
    - Map Arrow keys to `moveSelection`.
    - Implement `Tab` key logic to skip `fixed` cells (move forward/backward through non-fixed cells).
    
    Update `SudokuCell.tsx`:
    - Implement "Roving TabIndex": `tabIndex={isSelected ? 0 : -1}`.
    - Use a `useEffect` to call `.focus()` on the cell when it becomes selected.
    - Add `onClick` handler to set the selected index (respecting 'Stay Selected' decision).
  </action>
  <verify>
    <automated>grep "tabIndex" src/components/grid/SudokuCell.tsx && grep "Tab" src/components/grid/SudokuGrid.tsx</automated>
  </verify>
  <done>Grid cells are focusable and navigable via keyboard, with Tab skipping fixed cells.</done>
</task>

</tasks>

<success_criteria>
- Clicking a cell makes it the active selection.
- Arrow keys move the selection within the 9x9 grid.
- Grid Warp works (e.g., Right on column 8 moves to column 0).
</success_criteria>
