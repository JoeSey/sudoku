---
phase: 02-solving-interface
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: 
  - src/store/useGameStore.ts
  - src/components/grid/SudokuGrid.tsx
  - src/components/grid/SudokuCell.tsx
  - src/index.css
autonomous: true
requirements: [PH2-03, PH2-04]
must_haves:
  truths:
    - "Pressing 1-9 sets the value of the selected cell."
    - "Row, column, and block of the selected cell are highlighted."
    - "All cells with the same value as the selected cell are highlighted."
  artifacts:
    - path: "src/index.css"
      provides: "highlight styling"
---

<objective>
Implement number input and the multi-layered highlighting system.
Prioritize performance using granular Zustand selectors for highlights.

Purpose: Enable solving and provide visual feedback.
Output: A solving interface with real-time highlights and number input.
</objective>

<context>
@.planning/PHASE_2/RESEARCH.md
@.planning/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Keyboard Number Input</name>
  <files>src/components/grid/SudokuGrid.tsx</files>
  <action>
    Update `SudokuGrid`'s `onKeyDown` handler:
    - Map keys '1'-'9' to `setCellValue(selectedCellIndex, value)`.
    - Map 'Backspace' and 'Delete' to clearing the value.
    Ensure `fixed` cells cannot be modified (already handled by store).
  </action>
  <verify>
    <automated>grep "123456789" src/components/grid/SudokuGrid.tsx</automated>
  </verify>
  <done>Numbers can be entered into the grid via keyboard.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Multi-Layered Highlights</name>
  <files>src/components/grid/SudokuCell.tsx, src/index.css</files>
  <action>
    Update `SudokuCell.tsx`:
    - Use selectors to determine `isRelated` (row/col/block) and `isIdentical` (same number).
    - Apply corresponding CSS classes using `classnames`.
    
    Update `index.css`:
    - Add styles for `.selected`, `.highlight-related`, and `.highlight-identical`.
    - Use transparency and layering to allow for "blended" highlights as per CONTEXT.md.
  </action>
  <verify>
    <automated>grep "highlight-related" src/components/grid/SudokuCell.tsx && grep "highlight-identical" src/components/grid/SudokuCell.tsx</automated>
  </verify>
  <done>Visual highlights accurately reflect the current selection and value.</done>
</task>

<task type="auto">
  <name>Task 3: Implement On-Screen Keypad</name>
  <files>src/components/ui/Keypad.tsx, src/App.tsx</files>
  <action>
    Create a `Keypad` component with buttons for 1-9 and a clear action.
    - Clicking a button calls `setCellValue(selectedCellIndex, value)`.
    - Provide a toggle for "Note" mode (Phase 3 placeholder).
    Mount the `Keypad` in `App.tsx` below the grid for a "Hybrid" experience.
  </action>
  <verify>
    <automated>grep "Keypad" src/App.tsx</automated>
  </verify>
  <done>On-screen keypad is functional and integrated.</done>
</task>

</tasks>

<success_criteria>
- Users can fill in the grid with numbers 1-9 via physical keyboard or on-screen keypad.
- The "crosshair" highlight (row/col/block) updates instantly with selection.
- All identical numbers highlight when a cell is selected or a number is entered.
</success_criteria>
