---
phase: 01-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - package.json
  - tsconfig.json
  - src/types/index.ts
  - src/store/useGameStore.ts
  - src/utils/sudokuUtils.ts
autonomous: true
requirements: [PH1-01, PH1-02, PH1-03, PH1-06]
must_haves:
  truths:
    - "Project compiles with `npm run dev`"
    - "Store initializes with 81 empty cells"
    - "Puzzle generation produces valid 81-length arrays"
  artifacts:
    - path: "src/store/useGameStore.ts"
      provides: "Zustand store with `grid` and `initGame` action"
    - path: "src/types/index.ts"
      provides: "Cell, Difficulty, and GameState interfaces"
  key_links:
    - from: "src/store/useGameStore.ts"
      to: "sudoku-puzzle"
      via: "import for generation"
---

<objective>
Initialize the React + TypeScript environment and establish the core Zustand state management engine. 
Integrate the `sudoku-puzzle` library to handle logic generation.

Purpose: specific types and state shape must be defined before UI components can be built.
Output: A working store that can generate and hold a Sudoku puzzle in memory.
</objective>

<context>
@.planning/PHASE_1/RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Scaffolding & Dependencies</name>
  <files>package.json, tsconfig.json, vite.config.ts, src/index.css</files>
  <action>
    Initialize a Vite + React + TypeScript project (if not full initialized).
    Install dependencies: `zustand`, `immer`, `sudoku-puzzle`, `lucide-react`, `classnames`.
    Configure strict TypeScript settings in tsconfig.json.
    Reset src/index.css to a clean slate with a simple CSS reset.
  </action>
  <verify>
    <automated>npm install && npm run build</automated>
  </verify>
  <done>Dependencies installed, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Define Core Types</name>
  <files>src/types/index.ts</files>
  <action>
    Create the domain model based on Research recommendations:
    - `Cell`: { value: number | null, fixed: boolean, notes: number[] }
    - `Difficulty`: 'easy' | 'medium' | 'hard' | 'expert'
    - `GameState`: The store interface.
    Export these types for use in the store and components.
  </action>
  <verify>
    <automated>grep "interface Cell" src/types/index.ts</automated>
  </verify>
  <done>Types exported and valid.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Zustand Store & Validation</name>
  <files>src/store/useGameStore.ts, src/utils/sudokuUtils.ts</files>
  <action>
    Create the `useGameStore` using Zustand + Immer middleware.
    State:
    - `grid`: 1D array of 81 `Cell` objects.
    - `difficulty`: current difficulty level.
    Actions:
    - `initGame(difficulty)`: Uses `sudoku-puzzle` to generate a new puzzle.
      - Map the library's output (usually string or number array) to our `Cell` structure.
      - Set `fixed: true` for initial numbers.
    - `validateGrid()`: Uses `sudoku-puzzle`'s `isBoardValid` (or equivalent) to check for errors.
    - `resetGame()`: Resets mutable state but keeps the puzzle.
    
    *Note: Create a utility wrapper in `src/utils/sudokuUtils.ts` if `sudoku-puzzle` needs type casting.*
  </action>
  <verify>
    <automated>npx vite-node -e "import { useGameStore } from './src/store/useGameStore'; useGameStore.getState().initGame('easy'); if (useGameStore.getState().grid.length !== 81) process.exit(1); console.log('Grid init success');"</automated>
  </verify>
  <done>Store initializes a playable grid and exposes validation logic.</done>
</task>

</tasks>

<success_criteria>
- Project builds without errors.
- `useGameStore` can generate a valid 81-cell grid.
- Type safety is enforced via `src/types/index.ts`.
</success_criteria>
