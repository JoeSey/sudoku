---
phase: 03-smart-pencil-notes-and-fluidity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - src/types/index.ts
  - src/store/useGameStore.ts
  - src/components/grid/SudokuGrid.tsx
  - src/components/grid/SudokuCell.tsx
  - src/components/grid/PencilGrid.tsx
  - src/components/ui/Keypad.tsx
  - src/index.css
autonomous: true
requirements: [PH3-01, PH3-02]
must_haves:
  truths:
    - "Pressing 'N' toggles Note Mode."
    - "Note Mode is 'sticky' (stays active until toggled)."
    - "Pencil notes are rendered in a 3x3 fixed grid."
  artifacts:
    - path: "src/components/grid/PencilGrid.tsx"
      provides: "fixed 3x3 layout for candidates"
  key_links:
    - from: "src/store/useGameStore.ts"
      to: "src/components/grid/PencilGrid.tsx"
      via: "isNoteMode and notes data"
---

<objective>
Implement the "Note Mode" toggle logic and the fixed-position 3x3 pencil note visualization.
Prioritize the "Zen" aesthetic with subtle monochromatic styling and strict positioning.

Purpose: Essential UI foundation for pencil note management.
Output: A grid that supports a sticky note-taking mode with a fixed 3x3 layout.
</objective>

<context>
@.planning/PHASE_3/RESEARCH.md
@.planning/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Note Mode Toggle</name>
  <files>src/types/index.ts, src/store/useGameStore.ts, src/components/grid/SudokuGrid.tsx, src/components/ui/Keypad.tsx</files>
  <action>
    Update `GameState` type to include `isNoteMode: boolean`.
    Add `toggleNoteMode()` to store actions.
    
    Update `SudokuGrid.tsx`:
    - Map 'N' key to `toggleNoteMode()`.
    
    Update `Keypad.tsx`:
    - Update the Pencil Icon to show an "Active" state when `isNoteMode` is true.
    - Add a click handler to the icon to toggle the mode.
  </action>
  <verify>
    <automated>npx vite-node -e "import { useGameStore } from './src/store/useGameStore'; useGameStore.getState().toggleNoteMode(); if (!useGameStore.getState().isNoteMode) process.exit(1); console.log('Note mode toggle success');"</automated>
  </verify>
  <done>Note mode is togglable via keyboard and UI icon.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Fixed-Position Pencil Grid</name>
  <files>src/components/grid/PencilGrid.tsx, src/components/grid/SudokuCell.tsx, src/index.css</files>
  <action>
    Create `PencilGrid.tsx`:
    - Render a 3x3 CSS Grid with 9 slots.
    - Each slot (1-9) is rendered with `visibility: hidden` if the number is not in the cell's `notes`.
    
    Update `SudokuCell.tsx`:
    - Render `PencilGrid` if the cell is empty (`value === null`).
    - Pass the cell's `notes` array to the `PencilGrid`.
    
    Update `index.css`:
    - Implement subtle monochromatic styling for notes (smaller, lighter color).
    - Ensure `fixed` 3x3 grid alignment within the cell.
  </action>
  <verify>
    <automated>grep "grid-template-columns: repeat(3, 1fr)" src/index.css && grep "PencilGrid" src/components/grid/SudokuCell.tsx</automated>
  </verify>
  <done>Pencil notes are rendered in a stable, fixed 3x3 layout.</done>
</task>

</tasks>

<success_criteria>
- Toggling Note Mode works via 'N' and UI icon.
- Note mode state is "sticky" and visually indicated.
- Pencil notes (1-9) always appear in the same relative position within a cell.
- Pencil notes are styled subtly and do not clutter the grid.
</success_criteria>
