---
phase: 03-smart-pencil-notes-and-fluidity
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: 
  - src/types/index.ts
  - src/store/useGameStore.ts
  - src/components/grid/SudokuGrid.tsx
  - src/components/grid/SudokuCell.tsx
  - src/index.css
autonomous: true
requirements: [PH3-04]
must_haves:
  truths:
    - "User can select a range of cells by clicking and dragging."
    - "Only the most recently selected cell shows the 'primary' crosshair."
    - "Multiple cells can be active simultaneously."
  artifacts:
    - path: "src/store/useGameStore.ts"
      provides: "selectedIndices array instead of single index"
---

<objective>
Refactor the selection system to support multi-cell "Click & Drag" interaction.
Prioritize fluidity in group selection while maintaining focus on a "primary" cell.

Purpose: Enable bulk operations for pencil notes.
Output: A grid where users can select multiple cells by dragging.
</objective>

<context>
@.planning/PHASE_3/RESEARCH.md
@.planning/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Selection to Multi-Cell</name>
  <files>src/types/index.ts, src/store/useGameStore.ts</files>
  <action>
    Update `GameState`:
    - Change `selectedCellIndex: number | null` to `selectedIndices: number[]`.
    - Add `primaryIndex: number | null` to track the crosshair focus.
    
    Update store actions:
    - `setSelectedCellIndex` -> `setSelection(indices: number[])`.
    - `moveSelection` -> Update to clear multi-selection and move only the `primaryIndex`.
  </action>
  <verify>
    <automated>npx vite-node -e "import { useGameStore } from './src/store/useGameStore'; useGameStore.getState().setSelection([0, 1, 2]); if (useGameStore.getState().selectedIndices.length !== 3) process.exit(1); console.log('Multi-selection success');"</automated>
  </verify>
  <done>Store supports multiple selected cells simultaneously.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Click & Drag Interaction</name>
  <files>src/components/grid/SudokuGrid.tsx, src/components/grid/SudokuCell.tsx, src/index.css</files>
  <action>
    Update `SudokuGrid.tsx`:
    - Track `dragStart` and `isDragging` state.
    - `onMouseDown`: Set `dragStart` and initial selection.
    - `onMouseEnter` (delegated from cells): Update selection to a rectangular range between `dragStart` and current cell.
    - `onMouseUp`: End dragging.
    
    Update `SudokuCell.tsx`:
    - Use `selectedIndices.includes(index)` for the "selected" class.
    - Ensure only `primaryIndex === index` gets the crosshair highlights.
    
    Update `index.css`:
    - Add styling for multi-selected cells (distinct from primary selection).
  </action>
  <verify>
    <automated>grep "isDragging" src/components/grid/SudokuGrid.tsx</automated>
  </verify>
  <done>Users can select ranges of cells by dragging the mouse.</done>
</task>

</tasks>

<success_criteria>
- Clicking and dragging across the grid selects multiple cells.
- The "crosshair" highlight only shows for the primary/focused cell.
- Multi-selection works for both mouse (drag) and keyboard (refactored navigation).
</success_criteria>
