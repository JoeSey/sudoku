---
phase: 03-smart-pencil-notes-and-fluidity
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: 
  - src/store/useGameStore.ts
  - src/components/grid/SudokuGrid.tsx
  - src/components/ui/Keypad.tsx
autonomous: true
requirements: [PH3-03, PH3-04]
must_haves:
  truths:
    - "Entering a value ('5') auto-removes '5' as a candidate from related cells."
    - "Entering a note ('5') in Note Mode applies it to all selected cells (Bulk Add)."
    - "Auto-removed notes are not automatically restored when a value is cleared."
  artifacts:
    - path: "src/store/useGameStore.ts"
      provides: "smart cleanup and bulk toggle logic"
---

<objective>
Implement the "Smart Assistance" logic for auto-cleanup and the "Bulk Add" feature for pencil notes.
Prioritize speed and reduced cognitive load for the user.

Purpose: The core "Smart" features that differentiate the app.
Output: A fluid interface that handles candidate management automatically.
</objective>

<context>
@.planning/PHASE_3/RESEARCH.md
@.planning/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Smart Auto-Cleanup with Suspended Notes</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    Update `setCellValue` in the Zustand store:
    - Target only the `primaryIndex` for value entry when multiple cells are selected.
    - If a non-null value is set:
      - **Suspend Notes:** Do NOT clear the cell's own `notes` array (keep them in state but hidden by UI logic in SudokuCell).
      - **Smart Cleanup:** Identify all *other* related cells (row, column, block).
      - For each related cell, remove the new value from its `notes` array.
    - Add a `lastCleanedIndices: number[]` to state to trigger visual feedback.
  </action>
  <verify>
    <automated>npx vite-node -e "import { useGameStore } from './src/store/useGameStore'; useGameStore.getState().initGame('easy'); useGameStore.getState().grid[1].notes = [5]; useGameStore.getState().grid[0].notes = [9]; useGameStore.getState().setSelection([0]); useGameStore.getState().setCellValue(0, 5); const state = useGameStore.getState(); if (state.grid[1].notes.includes(5) || !state.grid[0].notes.includes(9)) process.exit(1); console.log('Smart cleanup (suspend) success');"</automated>
  </verify>
  <done>Pencil notes are cleaned up in related cells while being preserved in the active cell.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Visual Feedback for Cleanup</name>
  <files>src/components/grid/SudokuCell.tsx, src/index.css</files>
  <action>
    Update `SudokuCell.tsx`:
    - Use a selector to check if the cell's index is in `lastCleanedIndices`.
    - Apply a transient `.cleaned-pulse` class.
    
    Update `index.css`:
    - Implement a subtle fade/pulse animation for `.cleaned-pulse`.
  </action>
  <verify>
    <automated>grep "cleaned-pulse" src/components/grid/SudokuCell.tsx</automated>
  </verify>
  <done>Users receive subtle visual feedback when notes are auto-cleaned.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Bulk Note Application</name>
  <files>src/store/useGameStore.ts, src/components/grid/SudokuGrid.tsx, src/components/ui/Keypad.tsx</files>
  <action>
    Update `useGameStore.ts`:
    - Add `toggleNoteInSelection(note: number)` action.
    - Apply the note toggle to all cells in `selectedIndices` (ignoring fixed cells and cells with values).
    
    Update input handlers (Keyboard in `SudokuGrid`, Click in `Keypad`):
    - When in Note Mode, call `toggleNoteInSelection` instead of `setCellValue`.
  </action>
  <verify>
    <automated>grep "toggleNoteInSelection" src/store/useGameStore.ts</automated>
  </verify>
  <done>Users can add/remove candidates to multiple cells at once.</done>
</task>

</tasks>

<success_criteria>
- Placing a number instantly removes that number from candidates in the same row, column, and block.
- In Note mode, numbers (1-9) are toggled as candidates for all selected cells.
- The system feels "smart" and reduces the need for manual note management.
</success_criteria>
